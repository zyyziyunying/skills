interface:
  display_name: "Flutter Best Layout"
  short_description: "Simple Flutter layouts with focused guardrails"
  default_prompt: "Use $flutter-best-layout to choose the simplest suitable Flutter widget pattern while avoiding unnecessary fixed sizing, nested scrolling, and lost interaction semantics."

policy:
  allow_implicit_invocation: true
