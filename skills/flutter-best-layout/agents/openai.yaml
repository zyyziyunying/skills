interface:
  display_name: "Flutter Best Layout"
  short_description: "Flutter responsive UI layout and preview guidance"
  default_prompt: "Use $flutter-best-layout to implement, review, or refactor a Flutter UI layout, including responsive constraints, overflow, fixed design canvas adaptation, and allowed validation."

policy:
  allow_implicit_invocation: true
