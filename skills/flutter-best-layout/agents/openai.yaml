interface:
  display_name: "Flutter Best Layout"
  short_description: "Plan, review, preview, and implement Flutter UI layouts"
  default_prompt: "Use $flutter-best-layout to plan, review, preview, or implement a Flutter UI flow or responsive layout; follow project guidance for allowed validation."

policy:
  allow_implicit_invocation: true
