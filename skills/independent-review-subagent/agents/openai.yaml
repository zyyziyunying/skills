interface:
  display_name: "Independent Review Subagent"
  short_description: "Run no-context-fork review subagents"
  default_prompt: "Use $independent-review-subagent to open a no-context-fork subagent that independently reviews only the scoped target."
policy:
  allow_implicit_invocation: false
