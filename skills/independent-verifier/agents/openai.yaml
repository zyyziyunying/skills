interface:
  display_name: "Independent Verifier"
  short_description: "Code and docs review, bug triage, and test verification"
  default_prompt: "Use $independent-verifier to review this target for correctness, overengineering, and documentation redundancy, or triage bugs and design or verify behavior-focused tests with proportionate independence."
policy:
  allow_implicit_invocation: true
